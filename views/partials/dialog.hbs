<div class="mdc-dialog" id="{{id}}">
    <div class="mdc-dialog__container">
        <div class="mdc-dialog__surface" role="alertdialog" aria-modal="true" aria-labelledby="my-dialog-title"
            aria-describedby="my-dialog-content">
            {{#if dialogtitle}}
                <h2 class="mdc-dialog__title" id="my-dialog-title">{{dialogtitle}}</h2>
            {{/if}}
            <div class="mdc-dialog__content" id="{{id}}-content">
                {{question}}
            </div>
            <div class="mdc-dialog__actions">
                {{#if abort}}
                    <button type="button" class="mdc-button mdc-dialog__button" data-mdc-dialog-action="cancel">
                        <div class="mdc-button__ripple"></div>
                        <span class="mdc-button__label">Abbrechen</span>
                    </button>
                {{/if}}
                <button type="button" class="mdc-button mdc-dialog__button" data-mdc-dialog-action="ok">
                    <div class="mdc-button__ripple"></div>
                    <span class="mdc-button__label">Bestätigen</span>
                </button>
            </div>
        </div>
    </div>
</div>