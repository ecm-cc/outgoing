{{!-- <div class="mdc-text-field-helper-line helper-div">
    <div class="mdc-text-field-helper-text mdc-text-field-helper-text--persistent">
        {{title}}
    </div>
</div> --}}
<label class="mdc-text-field mdc-text-field--outlined property-field">

    <input type="text" class="mdc-text-field__input" aria-labelledby="{{id}}-label" id="{{id}}"
        value="{{#if value}}{{value}}{{/if}}">

    <div class="mdc-notched-outline">
        <div class="mdc-notched-outline__leading"></div>
        <div class="mdc-notched-outline__notch">
            <span class="mdc-floating-label" id="{{id}}-label">{{title}}</span>
        </div>
        <div class="mdc-notched-outline__trailing"></div>
    </div>
    <i class="material-icons mdc-text-field__icon mdc-text-field__icon--trailing" onclick="search()" tabindex="0" role="button">search</i>

</label>